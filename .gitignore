# dependencies
node_modules/

# local server state (start.sh / stop.sh)
.server.pid
.server.log

# macOS
.DS_Store

# Capacitor native projects — generated by `npx cap add ios`
/ios/
/android/

# editor / tooling
.vscode/
.idea/
*.swp

# NOTE: www/index.html is a build artefact but IS committed on purpose.
# It is the deployable single file — the whole point of the project — so it has
# to be present for anyone who clones and serves the repo without running a
# build. `./build.sh --check` verifies it matches src/.

.tunnel.log
.tunnel.pid
